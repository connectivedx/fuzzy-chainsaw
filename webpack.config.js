/*
  Configures webpack to build the site for development.

  This file imports the webpack config factory which is useful for developing by default.
*/

module.exports = require('./webpack.factory')();
