export const RichText =
  FcUtils.createBasicComponent({
    name: 'RichText',
    variants: ['default'],
    defaultProps: {
      tagName: 'div',
      variant: 'default'
    }
  });

export default RichText;
